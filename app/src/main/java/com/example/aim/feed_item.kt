package com.example.aim

import android.media.Image
import java.util.Date

class feed_item(date: Date, username: String, image: Image) {
    var date: Date = date
    var username: String = username
    var image: Image = image
}

